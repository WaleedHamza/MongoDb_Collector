<nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">MongoDB Collector</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                  <ul class="navbar-nav">
                    <li class="nav-item active">
                      <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link" href="/saved">Saved Articles</a>
                    </li>
                    {{!-- <li class="nav-item">
                      <a class="nav-link"  href="/scraped"><button type="button" class="btn btn-warning" id="grabBtn">Grab Data</button></a>
                    </li> --}}
                  </ul>
                </div>
              </nav>

              <div class="jumbotron jumbotron-fluid">
                    <div class="container">
                      <h1 class="display-2">MongoDB Collector</h1>
                      <p class="display-4 ">Collect Responsibly</p>
                    </div>
                  </div>

                  <div class="conatiner savedArticles">
                    {{#each dbArticle}}
                      {{#if this.saved}}
                        <div class="container">
                          <div class="article">
                            <div class="title"><a ><h4>{{title}}</h4></a></div>
                              <div class="actionBtns">
                                <button class="link btn btn-link"><a href="https://www.reddit.com{{this.link}}" target="_blank">View Article</a></button>
                                <button class="saveBtn btn btn-secondary btn-sm" data-id={{this.id}} data-toggle="modal" data-target="#articleSaveModal"><a>Add a Note</a></button>
                              </div>
                            </div>
                          </div>
                        {{/if}}
                      {{/each}}
                  </div>